<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>笑わ家 予約</title>
  <style>
    body {
      font-family: sans-serif;
      margin: 20px;
      padding: 0;
      background: #f9f9f9;
    }
    h1 {
      font-size: 1.4em;
      margin-bottom: 1em;
    }
    label {
      display: block;
      margin-top: 1.5em;
      font-weight: bold;
    }
    input[type="date"] {
      font-size: 1.2em;
      padding: 0.4em;
      width: 100%;
    }
    button {
      display: none; /* ボタン削除 */
    }
  </style>
</head>
<body>
  <h1>ご希望の日付を選んでください</h1>
  <form id="calendarForm">
    <label for="date">予約日</label>
    <input type="date" id="date" name="date" min="<%= days[0] %>" max="<%= days[days.length - 1] %>">
  </form>
  <script>
    document.getElementById("date").addEventListener("change", function () {
      if (this.value) {
        window.location.href = "/day/" + this.value;
      }
    });
  </script>
</body>
</html>
