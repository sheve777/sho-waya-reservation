<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>笑わ家 予約</title>
  <style>
    body { font-family: sans-serif; padding: 1em; text-align: center; }
    h1 { font-size: 1.5em; }
    ul { list-style: none; padding: 0; }
    li { margin: 0.5em 0; }
    a.button {
      display: inline-block;
      padding: 1em;
      margin: 0.3em;
      border: none;
      background-color: #d9534f;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-size: 1em;
    }
    @media (max-width: 480px) {
      a.button { width: 90%; display: block; margin: 0.5em auto; }
    }
  </style>
</head>
<body>
  <h1>笑わ家 予約カレンダー</h1>
  <ul>
    <% days.forEach(day => { %>
      <li><a class="button" href="/day/<%= day %>"><%= day %></a></li>
    <% }) %>
  </ul>
</body>
</html>