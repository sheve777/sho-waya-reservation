<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>予約時間の選択 - <%= date %></title>
  <style>
    body {
      font-family: sans-serif;
      padding: 2em;
      background-color: #f5f5f5;
    }
    h1 {
      color: #333;
    }
    form {
      margin-bottom: 1em;
      padding: 1em;
      background: white;
      border-radius: 5px;
      box-shadow: 0 0 5px rgba(0,0,0,0.1);
    }
    label {
      display: block;
      margin-top: 0.5em;
    }
    input[type="text"],
    input[type="number"] {
      padding: 0.5em;
      width: 100%;
      max-width: 300px;
    }
    .slot-buttons {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5em;
      margin-top: 1em;
    }
    button {
      padding: 0.6em 1em;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <h1>予約（<%= date %>）</h1>

  <form id="reservation-form" method="POST" action="/reserve">
    <input type="hidden" name="date" value="<%= date %>">

    <label>お名前：
      <input type="text" name="name" required>
    </label>

    <label>人数（1〜8名）：
      <input type="number" name="guests" min="1" max="8" required>
    </label>

    <label>席タイプ：
      <input type="radio" name="seatType" value="counter" checked> カウンター
      <input type="radio" name="seatType" value="table"> テーブル
    </label>

    <label>時間を選択してください：</label>
    <div class="slot-buttons">
      <% slots.forEach(slot => { %>
        <button type="submit" name="time" value="<%= slot %>"><%= slot %></button>
      <% }) %>
    </div>
  </form>
</body>
</html>
