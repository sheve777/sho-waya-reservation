<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>予約完了</title>
  <style>
    body { font-family: sans-serif; padding: 2em; text-align: center; }
    h2 { font-size: 1.8em; color: #2e8b57; }
    p { font-size: 1.2em; margin: 1em 0; }
    a {
      display: inline-block;
      margin-top: 1.5em;
      text-decoration: none;
      color: #fff;
      background: #007acc;
      padding: 0.6em 1.2em;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <h2>予約が完了しました！</h2>
  <p><%= date %> <%= time %> / <%= name %>（<%= people %>名 / <%= seatType === 'table' ? 'テーブル' : 'カウンター' %>）</p>
  <a href="/">トップに戻る</a>
</body>
</html>
